# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
# Optional. Canonical project env is `.envrc` / `use flake` via flake/devshells.nix.
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
allow_unfree: true
